export interface ButtonWhatsAppProps {
  /** prefilled chat message, defaults to a generic enquiry */
  message?: string
  /** placed in the bottom right corner next to the scroll to top button */
  offsetTop?: boolean
}